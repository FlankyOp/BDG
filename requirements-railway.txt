# Railway.app Deployment Dependencies
# Smaller footprint - torch not needed for data collection
# (LSTM training happens later in Google Colab)

requests>=2.28.0
firebase-admin>=6.0.0
typing_extensions>=4.8.0
types-requests>=2.31.0
